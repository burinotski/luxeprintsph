import React from 'react';
import './App.css';   // Keep this for styles (if any)
import LoginPage from './LoginPage'; // Import your LoginPage component

function App() {
  return (
    <div className="App">
      <LoginPage />   {/* Add the LoginPage component here */}
    </div>
  );
}

export default App;
